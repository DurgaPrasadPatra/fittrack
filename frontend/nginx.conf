server {
    listen 80;

    # Serve the static page
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Proxy /api/* to bmi-service (the trailing slashes strip the /api prefix)
    location /api/ {
        proxy_pass http://bmi-service:8000/;
    }
}
